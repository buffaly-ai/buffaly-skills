# LeadTags.pts

Defines guarded Feeding Frenzy lead-tag mutation wrappers. These actions inherit from `FeedingFrenzyJsonWsGuardedWriteAction` so they are not exposed through the default read-oriented skill action root.