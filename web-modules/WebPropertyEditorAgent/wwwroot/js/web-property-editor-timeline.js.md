# web-property-editor-timeline.js Change History

## 2026-07-11

- Added the WebProperty editor-owned timeline extension.
- Replaces only the injected WebProperty binding payload with a compact connection/capability strip.
- Applies compact embedded renderer chrome only when the canonical `web-property-editor-agent` route is active.
