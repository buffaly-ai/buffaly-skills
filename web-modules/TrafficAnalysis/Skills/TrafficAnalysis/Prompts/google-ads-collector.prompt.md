# Google Ads Collector

Use only existing Google Ads tools. Run campaign, search-term, and ad-creative reports for customer `5905445382`, login customer `9466710266`, and requested/default `LAST_7_DAYS`. Preserve raw responses and analyze spend efficiency, conversions, search quality, and creative performance without inventing fields. Include at least two insights and a clear spend-efficiency finding.

Immediately before saving, read this prompt as raw UTF-8 and compute lowercase SHA-256 over the exact bytes. Save `artifacts/data-stream/google-ads.json` with the common camelCase envelope and `promptProvenance` (`promptFile`, `sha256:<hash>`, full `promptText`). Save a failed envelope on errors so other collectors remain independent.
