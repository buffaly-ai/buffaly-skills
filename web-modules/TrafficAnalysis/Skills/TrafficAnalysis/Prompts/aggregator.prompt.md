# Traffic Analysis Aggregator

Read all available current-session files under `artifacts/data-stream/`: `traffic-properties.json`, `google-ads.json`, `linkedin-analytics.json`, `openai-usage.json`, and `reddit-ads.json`. Do not require all five.

Create cross-source synthesis with an executive summary, evidence-based insights, and prioritized actions. Use a constructive, opportunity-focused executive tone: lead with specific progress supported by evidence, identify gains or favorable signals that can be capitalized on, and state the most important improvements still needed. Be positive without hype, flattery, cheerleading, or unsupported praise. Do not minimize weak results, failed sources, wasted spend, or unresolved risks. Explicitly compare human traffic with AI/search/generic bot traffic when traffic data exists. Record every expected input with `ok`, `partial`, `failed`, or `missing` status. Do not invent metrics for unavailable sources.

Save only `artifacts/traffic-summary.json` as the `intelligence-factory-traffic-email.v1` report view model. The module owns HTML rendering; do not generate `traffic-summary-email.html` or any presentation markup. Use exactly these top-level properties: `templateVersion`, `reportLabel`, `timeframe`, `headline`, `executiveSummary`, `actions`, `visualizations`, `sources`, `footer`, and `promptProvenance`. Set `templateVersion` to `intelligence-factory-traffic-email.v1`. Add up to three useful, evidence-backed visualizations when comparable numeric magnitudes exist; omit decorative readiness/count charts. The v1 renderer does not support signed axes: every visualization `value` must be a non-negative magnitude. Put signed changes or deltas in source metrics/insights instead of a visualization. Each visualization must contain `key`, `title`, `description`, `unit`, and `items`; each item must contain `label`, numeric non-negative `value`, and formatted string `displayValue`. Each source must contain `key`, `name`, `status`, `summary`, `metrics`, `insights`, and `diagnostic`. Each metric must contain string `label` and string `value`. Include all five expected sources in stable order, including failed/missing sources with empty metrics and an available diagnostic. Keep values presentation-neutral: no HTML, Markdown, CSS, SVG, or template tokens.

Immediately before saving, read this prompt as raw UTF-8 and compute lowercase SHA-256 over the exact bytes. Store `promptProvenance` with `promptFile`, `promptHash` as `sha256:<lowercase hash>`, and full `promptText`.
