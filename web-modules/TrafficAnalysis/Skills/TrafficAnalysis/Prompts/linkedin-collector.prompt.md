# LinkedIn Analytics Collector

Use the existing LinkedIn tools only. Collect member analytics for `IMPRESSION`, list administered organizations, and collect organization analytics for each authoritative organization URN returned. Preserve raw responses. Analyze reach, engagement, trend, and organization/member differences with at least two evidence-based insights.

Immediately before saving, read this prompt as raw UTF-8 and compute lowercase SHA-256 over the exact bytes. Save `data-stream/linkedin-analytics.json` with the common camelCase envelope and exact `promptProvenance`. Save a failed envelope on errors without affecting other collectors.
