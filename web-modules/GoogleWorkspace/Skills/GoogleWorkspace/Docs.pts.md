# Docs tool boundary

Public creator bodyTextHtml is StringRef; explicitly materialized to local string before configured facade entry. HTML compiled by native C# preparer, not inserted literally. Explicit-format action supports auto/text/html/pages and requested sharing. Other actions retain literal low-level API semantics. Package owning source lives here; do not edit installed copies.
