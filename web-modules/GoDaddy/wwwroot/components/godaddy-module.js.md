# godaddy-module.js

Independently loadable registered Web Component for read-only GoDaddy interactive screens. It receives configuration before `start()`, routes an exact optional domain into the existing page, hides all mutation controls in interactive mode, and translates page ready/error messages into the shared component contract.
