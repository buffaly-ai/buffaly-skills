# ExtensionBrowserBound
Package-owned actions for the ExtensionBrowser WebModule. Production actions resolve the current session's opaque binding and never accept ports, credentials, installation IDs, or session keys.
