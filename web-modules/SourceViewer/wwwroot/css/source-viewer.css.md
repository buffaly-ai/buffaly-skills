# SourceViewer styles

Owns the standalone SourceViewer shell, CodeMirror token presentation, and bounded directory-list layout. Directory rows expose only type and name, preserve long-name truncation, distinguish disabled entries, and scroll within the viewer. Full-height source rendering, responsive actions, and operating-system light/dark themes remain supported.