# source-viewer.js

Loads one module-authorized source file through `/api/buffaly.source-viewer/source-file`, initializes CodeMirror read-only with `javascript`, `text/x-csharp`, or plain text mode, and supports copy/close only. It uses the endpoint's canonical camelCase JSON serialization.
