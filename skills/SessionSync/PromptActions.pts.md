# SessionSync PromptActions.pts Change History

## Prompt Rename Cleanup (2026-04-15)
- Renamed the previous-session prompt path from `SearchPreviousOpsSessions.prompt.md` to `SearchPreviousSessions.prompt.md`.
- Kept the prompt action prototype aligned with the cleaner `PreviousSessions` naming and removed stray user-facing `Ops` wording from the prompt title/body where it was just wrapper noise.
- Design: prompt behavior stayed the same; only the SessionSync prompt naming surface was cleaned up.
