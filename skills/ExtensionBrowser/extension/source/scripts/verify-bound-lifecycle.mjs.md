# verify-bound-lifecycle.mjs

Deterministic source contract for extension-owned authorization, exact extension-page sender trust (including tab-associated side panels), explicit missing-worker-response handling, single-flight channel reconnect, composite invocation completion, immutable per-conversation creation, compact iframe routing, and credential isolation from the side-panel component.
