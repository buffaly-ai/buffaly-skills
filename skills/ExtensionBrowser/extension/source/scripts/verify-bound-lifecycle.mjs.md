# verify-bound-lifecycle.mjs

Deterministic source contract for extension-owned authorization, single-flight channel reconnect, composite invocation completion, immutable per-conversation creation, compact iframe routing, and credential isolation from the side-panel component.
