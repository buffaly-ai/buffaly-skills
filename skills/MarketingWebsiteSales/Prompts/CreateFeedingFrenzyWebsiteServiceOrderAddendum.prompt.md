# CreateFeedingFrenzyWebsiteServiceOrderAddendum

> VALIDATED PROMPT ACTION ONLY
>
> Do not run this markdown file directly as ordinary prompt guidance.
> Call the ProtoScript action `ToCreateFeedingFrenzyWebsiteServiceOrderAddendumSkill` so the work prompt is followed by its validation prompt.

This file is a compatibility note only. The authoritative workflow body is `CreateFeedingFrenzyWebsiteServiceOrderAddendum.work.prompt.md`, and it must be executed through the validated action/tool path so validation runs before success.

