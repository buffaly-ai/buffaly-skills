# TwitterXApi Prompt Actions

Defines prompt-backed workflow actions for Twitter/X API operational setup.

- Added `TwitterXApiPromptAction` as the skill-owned prompt action base.
- Added `ToSetupTwitterXOAuth2AuthorizationSkill` to load the portal-token authorization runbook for configuring OAuth2 user-context tokens in the database-backed feature row.
