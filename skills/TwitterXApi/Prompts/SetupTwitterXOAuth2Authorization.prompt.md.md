# SetupTwitterXOAuth2Authorization.prompt.md

Runbook for setting up Twitter/X OAuth2 user-context authorization using the final successful portal-generated token method.

It guides the user through the existing X Developer App's User authentication settings, generating OAuth2 access/refresh tokens from the portal, saving them into the `Twitter/X OAuth2 Feature` database row, and validating with `ToGetXMe` without posting.
