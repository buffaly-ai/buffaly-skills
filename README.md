# Buffaly Skills

Official Buffaly skill directory proof-of-concept repository.

This repository is consumed by Buffaly Skill Directory Phase 1. The root `skills.index.json` is the metadata authority. Each listed skill folder contains an installable Buffaly skill with a top-level `index.pts` and explicitly listed payload files.
